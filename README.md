# Promo Builder

Static single-page site hosted at [promo-builder.com](https://promo-builder.com).

The entire app lives in `index.html`. Deployed via Vercel; every push to `main` publishes automatically.
